import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'practicing.ionic',
  appName: 'practicing-ionic',
  webDir: 'dist',
  server: {
    androidScheme: 'https'
  }
};

export default config;
